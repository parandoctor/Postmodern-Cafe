# v1.2.8

## 更新内容

首页双栏布局落地：左侧终端内容左对齐收窄，右侧新增「剪影摄影档案图」（去水印 + AI 超分，纯黑白灰档案风），替换 1.2.6 之后右侧纯黑留白。登录/注册页 UI 改版：卡片透明度大幅提升（bg-black/10 去毛玻璃）、背景压暗层调弱、输入框局部半透明衬底，新增柔和玻璃按钮 `.auth-btn`（鼠标远离为半透明玻璃态，靠近变白底黑字 + 双层光晕 + 上浮 + 扫描光）。修复多项问题：登录/注册输入框每敲一字失焦（Entrance 组件类型缓存）、背景音乐切换页面中断/不自动响起/首次点击卡顿（模块级单例 Audio + sessionStorage 持久化 + 预加载 + 交互补播）、登录/注册小视口下表单被裁（flex 居中 + 限高滚动）、hero/footer 内链硬跳转（原生 a 改 next/link）。

## 主要新增文件

- `public/art/silhouette-archive.png` — 首页右栏剪影摄影档案图（1200x1200）
- `docs/开发日志[2026.8.28-1.2.8].md` — 本次开发日志

## 主要修改

- `src/components/landing/hero.tsx` — 左右双栏布局 + CTA 改 next/link
- `src/components/landing/footer.tsx` — 快捷链接改 next/link
- `src/app/(auth)/login/page.tsx`、`register/page.tsx` — 高透明卡片 + 输入框衬底 + flex 居中限高布局
- `src/app/globals.css` — 新增 .auth-btn 柔和玻璃按钮样式
- `src/components/ui/auth-td-background.tsx` — 背景径向压暗层调弱
- `src/components/ui/default-bgm.tsx` — 模块级单例 Audio + sessionStorage 持久化 + 预加载 + 交互补播
- `src/components/ui/entrance.tsx` — useMemo 缓存组件类型，修复输入框失焦
- `package.json` — 版本号 1.2.7 -> 1.2.8
- `README.md` — 版本号与文档同步
- `docs/开发日志.md` — 版本记录表新增一行

## 其他

- 首页右栏图片处理：原图去除底部 Ikoko 水印（程序化定位 + 背景色填充 + 羽化），阿里百炼 Qwen Image AI 超分至 2048x2048，压缩 1200x1200 入库
- 验证：tsc / build 通过；CDP 实测输入框连续输入不失焦、BGM 10 秒采样无停滞无跳变、切页不中断、刷新续播、1280x700 小视口表单完整可见
