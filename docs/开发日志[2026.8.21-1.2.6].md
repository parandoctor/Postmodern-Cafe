# 开发日志 [2026.8.21 - 1.2.6]

## Plan / 策划

目标需求：
- 本次更新集中在首页设计创新：将落地页从"浅色天体蓝图风"整体改造为"黑底终端档案风"，与仓库七级黑白灰阶体系呼应
- 参考小红书黑白科技艺术素材（故障/数据流/粒子/实验排版）与用户提供的顶部设计参考图
- 顶部导航按参考图：左侧纯文字品牌，右侧实时时钟 + ARCHIVE.FS 标签 + 白色描边登录按钮，无图标
- Hero 左对齐终端风：ASCII 字标、终端命令、glitch 标题"一切终将归档"，替换旧标题
- 功能介绍改为终端档案表，联系方式改为终端频道列表，配两张黑白艺术图（构成主义几何 / 点阵人像）
- 右下角 BGM 控件改为终端风（方形描边 + 字符图标 + BGM 状态标签）
- 咖啡图标移出导航，仅作为 README 标题小图标
- 保持纯黑白灰配色，仅保留终端语义点缀；未改动登录注册页与后台系统

方案决策：
- 终端样式工具类（term-bg / term-scanlines / term-noise / term-glitch 等）统一加在 `globals.css`
- 黑白艺术配图用百炼 Qwen Image 生成，存 `public/art/`（种子已记录，可复现）
- Hero 采用左对齐布局贴合参考预览图；标题沿用 v1.2.6 方案 V3 的"一切终将归档"
- 本次版本号 1.2.5 -> 1.2.6

## Coding / 关键

新增文件：
- `public/art/features-geometry.png` — 构成主义几何黑白配图（功能区块，Qwen 生成）
- `public/art/contact-dotwork.png` — 点阵人像黑白配图（联系方式区块，Qwen 生成）
- `src/components/ui/entrance.tsx` — 滚动入场动画封装（落地页统一动画入口，随本次落地页提交）
- `docs/开发日志[2026.8.21-1.2.6].md` — 本次开发日志
- `docs/versions/1.2.6.md` — 版本说明文档

修改文件：
- `src/app/globals.css` — 新增终端档案风工具类（term-bg / term-dim / term-line / term-scanlines / term-noise / term-blink / term-glitch / term-caret 与 keyframes）
- `src/components/landing/navbar.tsx` — 按参考图重做：左侧纯文字品牌（后现代咖啡馆 :: POSTMODERN-CAFÉ），右侧实时时钟 + ARCHIVE.FS + 白色描边登录按钮，黑底终端风；咖啡图标移除
- `src/components/landing/hero.tsx` — 左对齐终端 Hero：状态行 + ASCII 字标 + 终端命令 + glitch 标题"一切终将归档" + 终端 CTA + 特性终端行
- `src/components/landing/features-section.tsx` — 功能模块改为终端档案表（MODE/MODULE/DESC/STATUS，文件型条目 upload.ts / archive/ 等），右侧构成主义几何配图
- `src/components/landing/footer.tsx` — 联系方式改为终端频道列表（./contact/xxx）+ 点阵人像配图 + 终端快速导航与版权状态行
- `src/components/ui/default-bgm.tsx` — BGM 控件终端化：黑底方框 + BGM:ON/OFF 标签 + 方形描边字符按钮（▶/❚❚、♪/✕）
- `README.md` — 标题加入咖啡小图标；版本号与目录树同步至 v1.2.6
- `package.json` — 版本号 1.2.5 -> 1.2.6
- `docs/开发日志.md` — 版本记录表新增一行

技术要点：
- JSX 文本中直接书写 "//" 会被 ESLint `react/jsx-no-comment-textnodes` 判为注释，需用表达式 `{"//"}` 包裹
- framer-motion Entrance 视口入场在无头截图下 IntersectionObserver 不触发，内容保持 opacity 0；真实浏览器滚动正常，验证改用 playwright 驱动真实滚动截图
- 配图统一 `grayscale` 滤镜保证纯黑白呈现

## Target / 目标需求

- 首页（导航 / Hero / 功能模块 / 联系方式 / BGM）整体统一为黑底终端档案风
- 顶部设计贴合参考图：左侧文字品牌、右侧时钟 + ARCHIVE.FS + 登录，无图标
- Hero 左对齐，新标题"一切终将归档 / EVERYTHING GETS ARCHIVED"
- 功能介绍与联系方式参考两张黑白艺术图重写
- 咖啡图标仅保留在 README 小图标位
- 纯黑白灰配色；`npx tsc --noEmit` 与 `npm run build` 通过

## Questions / 瓶颈点

- ESLint 对 JSX 中 "//" 文本报错，需表达式包裹，后续落地页代码需注意
- 无头截图无法触发滚动入场动画，视觉验证依赖 playwright 真实滚动；后续自动化截图建议统一走 playwright
- 桌面导航按参考图去掉了菜单项与注册按钮，注册入口仅在 Hero CTA 与移动端菜单，后续若需恢复可加实心注册按钮
- 登录注册页与后台系统仍为浅色 / 原主题，首页与后台存在明暗反差，属预期分层，是否统一待后续版本评估

## Follow / 优化拆解

1. 登录 / 注册页与后台系统是否统一为终端风（待用户确认）
2. BGM 控件可进一步字符化（如 SOUND:[ON] 文本态）
3. 首页 ASCII 字标可随品牌文案定制
4. 联系方式真实地址替换（GitHub / 邮箱 / QQ / Telegram 当前为占位）
5. 配图可加 WebP 版本减小体积
