# 开发日志 [2026.8.21 - 1.2.5]

## Plan / 策划

目标需求：
- 在 v1.2.4 基线上开发（本地 main 重置与 origin/main 对齐，清理旧分叉）
- 为 主页 / 登录注册页 / 后台系统 添加默认背景音乐（电机背景音乐）
- 提供播放 / 暂停 / 静音控制；进入页面自动尝试播放，被浏览器拦截时首次交互后自动续播
- 与音乐盒（本地曲库）互斥播放，避免两路音频同时出声
- 相关文档不出现歌曲名与来源，统一表述为"添加电机背景音乐"
- AGENTS.md 加入 .gitignore 并移出版本库索引，不上传云端
- 本地与远程保持一致：本地 main 基于 origin/main（v1.2.4），完成后推回 origin/main

方案决策：
- 音频资源放 `public/audio/bgm-default.m4a`，由 Next.js 静态托管
- 新建 `DefaultBgm` 客户端组件挂在根布局，三个区域统一生效且页面跳转不中断
- 新建 `src/store/bgm.ts` 全局播放状态，供默认背景音乐与音乐盒互斥联动
- 本次版本号 1.2.4 -> 1.2.5

## Coding / 关键

新增文件：
- `public/audio/bgm-default.m4a` — 默认背景音乐音频资源
- `src/components/ui/default-bgm.tsx` — 默认背景音乐播放器（播放 / 暂停 / 静音，自动播放兜底）
- `src/store/bgm.ts` — 背景音乐全局播放状态
- `docs/开发日志[2026.8.21-1.2.5].md` — 本次开发日志
- `docs/versions/1.2.5.md` — 版本说明文档

修改文件：
- `src/app/layout.tsx` — 根布局挂载 DefaultBgm
- `src/components/layout/sidebar-music.tsx` — 音乐盒开始播放时自动暂停默认背景音乐
- `.gitignore` — 新增 agents.md / AGENTS.md，防止上传云端
- `package.json` — 版本号 1.2.4 -> 1.2.5
- `README.md` — 同步版本号、项目简介、功能表与目录树
- `docs/开发日志.md` — 版本记录表新增一行
- `docs/版本更新流程.md` — 从旧 main 恢复（远端缺失，随本次提交同步）

## Target / 目标需求

- 主页 / 登录注册页 / 后台系统均默认播放背景音乐，可随时暂停或静音
- 页面间跳转不中断背景音乐
- 音乐盒播放本地音乐时自动暂停默认背景音乐，避免串音
- 文档不含歌曲名与来源
- 本地 main 与 origin/main 基于同一基线并保持一致

## Questions / 瓶颈点

- 浏览器自动播放策略限制：首次访问无交互时无法出声，采用首次交互兜底补播
- 默认背景音乐与音乐盒共用一套播放状态，后续若支持多路音频需扩展状态模型
- 本地旧 main（1.1.01 线）与远端（1.2.4 线）分叉较大，本次直接重置对齐远端基线，旧提交保留在 backup/main-pre1.2.5 标签

## Follow / 优化拆解

1. 背景音乐可增加音量滑块与播放进度条
2. 可考虑按账号持久化"默认音乐开关"偏好
3. 音频文件体积约 9.6MB，后续可提供低码率版本按网络质量切换