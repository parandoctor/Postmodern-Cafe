# 开发日志 (开发隔离文档)

> 本文档用于记录开发过程，与 README.md（面向用户的使用文档）相互独立。
> 规则：不包含 emoji，语言简洁，标注新增功能/文件的落点。

---

## 版本记录

| 版本 | 日期 | 摘要 |
|------|------|------|
| 1.1.0 | 2025-07-22 | 后台 UI 重做：黑白灰阶分类 + Notion 风格侧边栏 + 效率面板（待办/随手记/音乐盒/日历/计时器） |

---

## Plan / 策划

### v1.1.0：后台系统 UI 全面重做

目标需求：
- 取消七种彩色分类，改为黑白灰阶（不触碰数据库与服务端逻辑，只替换颜色值）
- 统一 Notion 风格：黑白简洁 + 立体感阴影
- 侧边栏按 Notion 风格重做，保留原有图标
- 删除左下角悬浮小球（光标拖尾 CursorTrail）及其功能
- 右侧新增日历、计时器
- 左侧新增每日待办、随手记、音乐盒（支持本地上传音乐播放）
- 背景壁纸：下架所有预设壁纸，仅保留默认浅色与自定义壁纸

方案决策：
- 保留 `RainbowColor` 类型键（red/orange/yellow/green/blue/darkblue/purple）与 `RAINBOW_COLORS` 记录结构，
  仅替换 hex 为黑白灰阶值，避免改动数据库、服务端 actions、zod 校验与 `categories/[color]` 路由。
- 音乐曲库用 IndexedDB 持久化（localStorage 容量不足以存音频 dataUrl）。
- 浅灰阶颜色下图标/文字对比度不足，新增 `getContrastColor()` 自动对比函数。

---

## Coding / 关键

### v1.1.0 关键修改

新增文件：
- `src/lib/idb.ts` — IndexedDB 封装（DB: rainbow-box，store: music-tracks），音乐曲库持久化
- `src/store/widgets.ts` — 三个 zustand store：`useTodoStore`（每日待办）、`useNotesStore`（随手记）、`useMusicStore`（音乐盒）
- `src/components/layout/sidebar-todo.tsx` — 每日待办组件（增删、完成勾选、清除已完成）
- `src/components/layout/sidebar-notes.tsx` — 随手记组件（新增、点击内联编辑、删除）
- `src/components/layout/sidebar-music.tsx` — 音乐盒组件（本地上传、播放/暂停、删除）
- `src/components/layout/calendar-widget.tsx` — 日历组件（月切换、今日/选中高亮）
- `src/components/layout/timer-widget.tsx` — 计时器组件（秒表/倒计时、预设时长、到点提示）
- `docs/versions/1.1.0.md` — 版本说明文档

修改文件：
- `src/app/globals.css` — 主题变量全部改为黑白灰阶；新增 `.border-whisper`、`.shadow-notion`、`.shadow-notion-deep`、`.shadow-notion-dark` 工具类；字体改为 Inter 体系
- `src/types/index.ts` — `RAINBOW_COLORS` 七色 hex/label/gradient 改为白→黑灰阶（键名不变）
- `src/store/index.ts` — UIStore 新增 `rightOpen` 状态（右侧面板开关）
- `src/components/layout/dashboard-layout.tsx` — 整体重写：
  - 侧边栏改为 Notion 风格（280px 宽、灰度高亮、工作区分组）
  - 左侧挂载待办/随手记/音乐盒
  - 右侧新增 300px 面板（日历 + 计时器），可开关
  - 壁纸弹窗下架预设壁纸，仅剩"默认浅色"与"自定义壁纸"
  - 移除 CursorTrail 引用
- `src/app/dashboard/categories/page.tsx` — 从全屏七色分栏改为黑白卡片网格（保留全部功能）
- `src/lib/utils.ts` — 新增 `getContrastColor()` 灰度对比色函数
- `src/app/dashboard/files/page.tsx`、`src/app/dashboard/favorites/page.tsx`、`src/components/categories/category-modal.tsx` — 分类徽章/图标改用对比色，保证浅灰阶可读
- `package.json` — 版本号 1.0.2 → 1.1.0
- `README.md` — 同步更新至 v1.1.0（用途文档角色）

删除文件：
- `src/components/ui/cursor-trail.tsx` — 悬浮小球组件，功能已按需求移除

---

## Target / 目标需求

### v1.1.0 目标（已达成）
- 后台全部改为黑白简洁风格，具备立体感
- 分类仅换色，功能不破坏（数据库/服务端/校验/路由均未改动）
- 侧边栏 Notion 化，保留原图标
- 悬浮小球及其功能删除
- 右侧日历 + 计时器
- 左侧每日待办 + 随手记 + 音乐盒（本地音乐上传播放）
- 壁纸仅默认浅色 + 自定义

### 未来方向
- 首页落地页 `categories-section.tsx` 仍展示七种彩色面板，可考虑同步改为灰阶
- 音乐盒后续可支持播放列表、进度条拖动、循环/随机模式
- 日历可扩展为点击日期记录事件
- 待办可支持编辑、截止日期提醒

---

## Questions / 瓶颈点

- 音乐文件以 base64 dataUrl 存入 IndexedDB，大文件占用存储较多；后续可考虑 IndexedDB 直接存 Blob 或引入本地文件句柄（File System Access API）
- 浅灰阶（白/浅灰）下分类图标与文字对比度需靠 `getContrastColor()` 动态处理，后续若引入更多自定义颜色需覆盖更复杂场景
- 右侧面板在 <lg 屏幕下默认隐藏，移动端未提供抽屉入口（仅桌面可用）
- 壁纸下架后，旧用户可能仍存有 wallpaper 的 preset 值，未做迁移清理

---

## Follow / 优化拆解

1. 首页落地页彩色分类区改灰阶（可选）
2. 音乐盒增强：Blob 存储、播放列表、进度拖动、循环模式
3. 日历事件记录（数据持久化）
4. 待办编辑与提醒
5. 移动端右侧面板抽屉入口
6. 旧壁纸 preset 数据清理
