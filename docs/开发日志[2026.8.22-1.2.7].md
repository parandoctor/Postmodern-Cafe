# 开发日志 [2026.8.22 - 1.2.7]

## Plan / 策划

目标需求：
- 登录 / 注册页视觉升级：背景采用 touchdesigner 黑白粒子抽象原图（小红书素材，直接使用参考图，不再程序化拼凑）
- 登录 / 注册框架采用 V4「碎形重叠卡片」方向：左对齐裁剪卡片 + 大写下划线字段 + 白底按钮 + 黑底粒子背景
- 去掉巨型描边数字 01 / 02（用户要求）
- 登录/注册功能逻辑不变：loginUser / registerUser、密码显隐、错误提示、loading
- 纯黑白灰体系，与首页终端风、后台 Notion 风保持一致
- 版本号 1.2.6 -> 1.2.7，沿用 1.2.6 分支为基线

方案决策：
- 背景图 `public/art/auth-td-particles.jpg`（1440x1080 原素材），由 `AuthTdBackground` 组件以 object-cover 全屏展示，灰度/对比微调 + 极慢 Ken Burns 缩放 + 极轻动态颗粒 + 中心压暗 + 暗角
- 登录/注册卡片 V4：absolute left 8% 垂直居中，clip-path 斜切角，Bahnschrift 粗体标题，输入框底部细线（border-b），全大写占位符
- 四个候选框架方案（V1 终端 / V2 瑞士分屏 / V3 目录表 / V4 碎形）先做静态原型 + 截图评审，用户选定 V4

## Coding / 关键

新增文件：
- `public/art/auth-td-particles.jpg` — 登录/注册背景底图（touchdesigner 黑白粒子抽象原图）
- `src/components/ui/auth-td-background.tsx` — 背景组件（原图 + Ken Burns + 颗粒 + 暗角 + 中心压暗）
- `docs/开发日志[2026.8.22-1.2.7].md` — 本次开发日志
- `docs/versions/1.2.7.md` — 版本说明文档

修改文件：
- `src/app/(auth)/layout.tsx` — 背景组件切换为 AuthTdBackground
- `src/app/(auth)/login/page.tsx` — V4 左对齐裁剪卡片（EMAIL / PASSWORD 下划线字段 + 进入按钮），移除 Logo 居中与巨型数字 01
- `src/app/(auth)/register/page.tsx` — V4 同款框架（NAME / EMAIL / PASSWORD 三字段 + 创建账户按钮），移除巨型数字 02
- `package.json` — 版本号 1.2.6 -> 1.2.7
- `README.md` — 徽章与标题版本号同步 v1.2.7
- `docs/开发日志.md` — 版本记录表新增一行

技术要点：
- 候选方案用静态 HTML 原型（frontend-design / hallmark 原则：避免模板化），截图经 VL 评审后选定 V4
- 卡片 clip-path 用 Tailwind 任意值/inline style 实现斜切角；巨型描边数字用 -webkit-text-stroke + transparent fill，但用户最终要求去掉
- 背景原图经 grayscale + contrast + brightness 滤镜微调，中心 radial 压暗保证卡片可读性

## Target / 目标需求

- 登录/注册背景为 touchdesigner 黑白粒子抽象图（与参考图一致）
- 登录/注册框架为 V4 左对齐裁剪卡片（无巨型数字）
- 纯黑白灰，卡片可读性良好
- `npx tsc --noEmit` 与 `npm run build` 通过

## Questions / 瓶颈点

- 巨型描边数字在原图高亮区域对比不足，最初需要加强描边；用户最终选择去掉
- 登录/注册页此前存在未提交改动，本次在其基础上重写，未纳入其他无关改动（.env.example 等）

## Follow / 优化拆解

1. 注册/登录表单可增加焦点高亮、键盘可达性细节
2. 背景 Ken Burns 动画可改为更克制的缓慢漂移
3. 后续可把 V1/V2/V3 候选方案作为未来 A/B 测试储备
